import { TestIdDirective } from './test-id.directive';

describe('TestIdDirective', () => {
  it('should create an instance', () => {
    const directive = new TestIdDirective();
    expect(directive).toBeTruthy();
  });
});
